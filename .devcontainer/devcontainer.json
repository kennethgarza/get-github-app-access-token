{
	"name": "Get_Github_App_Access_Token",
	// Set the build context one level higher so we can grab metadata like global.json
    "dockerComposeFile" : [
        "docker-compose.yml"
    ],
    "service" : "dev_env",
    "workspaceFolder": "/src"
}